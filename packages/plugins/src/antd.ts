import { IApi } from 'umi';

export default (api: IApi) => {
  api;

  // antd import
  // dark mode
  // compat mode
  // dayjs (by default?)
  // babel-plugin-import
  // antd config provider (HOLD, depends on umi)
};
