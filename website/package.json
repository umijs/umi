{
  "scripts": {
    "now-build": "npm run build && cd website && ls",
    "deploy": "npm run build && now && now alias",
    "build": "vuepress build ../docs --dest ./dist"
  },
  "devDependencies": {
    "vuepress": "^0.14.8"
  },
  "engines": {
    "node": "10.x"
  }
}
