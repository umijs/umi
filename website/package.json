{
  "name": "umi-site",
  "scripts": {
    "start": "node server.js",
    "deploy": "npm run build:doc && now -A ../now.json && now alias -A ../now.json",
    "dev": "vuepress dev ../docs",
    "build": "echo empty build",
    "build:doc": "vuepress build ../docs --dest ./build"
  },
  "dependencies": {
    "express": "^4.16.2"
  },
  "devDependencies": {
    "now": "^11.3.11",
    "vuepress": "^0.14.2"
  }
}
